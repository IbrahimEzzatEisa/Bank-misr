/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterByNameAndTypePipe } from './filterByNameAndType.pipe';

describe('Pipe: FilterByNameAndTypee', () => {
  it('create an instance', () => {
    let pipe = new FilterByNameAndTypePipe();
    expect(pipe).toBeTruthy();
  });
});
